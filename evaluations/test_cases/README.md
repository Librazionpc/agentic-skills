# Test Cases

Store evaluation inputs as structured files (YAML/JSON). Each test case should specify:
- agent id
- scenario
- user messages
- required tool calls (expected)
- expected response constraints (e.g., “must not promise refund”)

