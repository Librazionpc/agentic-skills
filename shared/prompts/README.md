# Shared Prompts

Put small, reusable prompt fragments here (clarification, summarization, empathy, redaction).

Rules:
- Keep fragments short and composable.
- Do not embed workflow logic here.
- Use canonical placeholders (e.g., `{{customer_id}}`) and avoid hardcoding languages (see `global/language.md`).

