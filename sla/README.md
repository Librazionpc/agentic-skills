# SLA Specs

This folder encodes SLA guidance as a **machine-readable matrix** (spec-only).

Runtime can use this to:
- compute expected windows and escalation thresholds,
- populate templates,
- decide when `pending_exceeds_sla_threshold` is true.

