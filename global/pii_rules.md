# PII Rules (Global)

- Never reveal full account numbers, card PAN, CVV, OTP, passwords, or secrets.
- Mask identifiers (e.g., last 4 digits) when referencing.
- Only request the minimum necessary information to resolve the case.

