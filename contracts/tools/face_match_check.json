{
  "version": 1,
  "name": "face_match_check",
  "description": "Read-only selfie vs ID face match result. Returns customer-safe match outcome only.",
  "request_schema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "required": ["selfie_ref", "document_ref"],
    "properties": {
      "selfie_ref": { "type": "string" },
      "document_ref": { "type": "string" }
    },
    "additionalProperties": false
  },
  "response_schema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "required": ["result"],
    "properties": {
      "result": { "type": "string", "enum": ["pass", "fail", "review"] },
      "reasons": { "type": "array", "items": { "type": "string" } }
    },
    "additionalProperties": true
  },
  "errors": ["INVALID_INPUT", "FORBIDDEN", "UPSTREAM_UNAVAILABLE"]
}

