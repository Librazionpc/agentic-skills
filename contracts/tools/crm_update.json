{
  "version": 1,
  "name": "crm_update",
  "description": "Controlled-write update to CRM notes/tags for collections status. Must redact secrets and avoid sensitive identifiers.",
  "request_schema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "required": ["customer_id", "update"],
    "properties": {
      "customer_id": { "type": "string" },
      "update": { "type": "object" }
    },
    "additionalProperties": false
  },
  "response_schema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "required": ["ok"],
    "properties": { "ok": { "type": "boolean" } },
    "additionalProperties": true
  },
  "errors": ["INVALID_INPUT", "FORBIDDEN", "RATE_LIMITED"]
}

